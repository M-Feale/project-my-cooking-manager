// Constants used for the BE

// Database name
const DB_NAME = "my_cooking_manager";

// User collection name
const US_COLL = "users";

// Recipe collection name
const RE_COLL = "recipes";

module.exports = {
    DB_NAME,
    US_COLL,
    RE_COLL
}